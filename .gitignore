# Node.js and React (Vite)
node_modules/
dist/         # Vite build output
.vite/        # Vite cache directory
*.local       # Local environment variables files (e.g., .env.local)
.env          # Environment variables file

# FastAPI and Python
__pycache__/
*.pyc
.pytest_cache/
.venv/        # Python virtual environment (if used in root or backend)
venv/         # Another common name for virtual environment
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/ # Jupyter notebooks checkpoints if you use them
*.sqlite3     # SQLite databases (if you use SQLite locally)
*.db          # Other database files
/backend/instance/ # Flask/Django instance folder, good to include for FastAPI too for local data
/backend/__pycache__/ # Specific to backend if it's nested
/backend/venv/ # Specific to backend if it's nested

# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
